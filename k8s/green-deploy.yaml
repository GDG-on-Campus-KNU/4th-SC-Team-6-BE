apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-green
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring
      color: green
  template:
    metadata:
      labels:
        app: spring
        color: green
    spec:
      containers:
        - name: feelody-back
          image: {TODO}
          ports:
            - containerPort: 8080

